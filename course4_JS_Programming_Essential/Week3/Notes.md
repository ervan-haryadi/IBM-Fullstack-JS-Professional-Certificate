use try catch block to for code flow debugging

on DOM, $0 refers to the currently focused element

DOM manipulation:
- access: getElementbyId ; getElementsByTagName ; getElementsByClassName ; querySelector (uses CSS-like selectors)